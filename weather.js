#!usr/bin/env node

import { getArgs } from './helpers/getArgs.js';

const initCLI = () => {
  const args = getArgs(process.argv.slice(2));

  if (args.s) {
  }
  if (args.h) {
  }
  if (args.t) {
  }
};

initCLI();
